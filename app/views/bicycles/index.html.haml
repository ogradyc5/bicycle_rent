%h1.text-center Available bicycles
%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Class
      %th Price/day
      %th{:colspan => "3"}
  %tbody
    - @bicycles.each do |bicycle|
      %tr
        %td= link_to bicycle.name, bicycle
        %td= bicycle.kind
        %td
          = bicycle.price
          zł
        %td= link_to 'Rent', new_bicycle_rental_path(bicycle), class: 'btn btn-success'
        %td
          = link_to 'Show', bicycle, class: 'btn btn-default'
          - if user_signed_in?
            = link_to 'Edit', edit_bicycle_path(bicycle), class: 'btn btn-info'
            = link_to 'Destroy', bicycle, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-danger'
- if user_signed_in?
  = link_to 'New Bicycle', new_bicycle_path, class: 'btn btn-success pull-right'